[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ExEv"
version = "0.4.0"
description = "Research simulator for dynamic evacuation routing and optimization"
requires-python = ">=3.11"

[project.scripts]
exev = "exev.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
